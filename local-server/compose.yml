name: letta

services:
  letta-server:
    image: letta/letta:latest
    volumes:
      - letta-pgdata:/var/lib/postgresql/data
    ports:
      - "8283:8283"
    env_file:
      - path: server.env
        required: true

volumes:
  letta-pgdata:
